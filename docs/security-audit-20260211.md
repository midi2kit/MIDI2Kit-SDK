# セキュリティ監査レポート

## 監査概要
- 対象: MIDI2Kit - Package.swift, Scripts/build-xcframework.sh
- 日付: 2026-02-11
- 監査範囲: シェルインジェクション、パストラバーサル、ファイル操作安全性

## エグゼクティブサマリー
- Critical: 0件
- High: 0件
- Medium: 1件
- Low: 4件
- Info: 2件
- 総合リスク評価: 低

---

## 発見事項

### [SEV-001] Low: 変数の不完全なクォーティング

**概要**
一部の変数展開がダブルクォートで囲まれておらず、スペースや特殊文字を含むパスで問題が発生する可能性があります。

**影響**
ビルドディレクトリやモジュール名にスペースが含まれる場合、ワード分割によりコマンドが誤動作する可能性があります。ただし、現在のハードコードされた値（`MIDI2Core`等）にはスペースは含まれません。

**場所**
- ファイル: `/Users/hakaru/Desktop/Develop/MIDI2Kit/Scripts/build-xcframework.sh`
- 行: 261-263, 268

**証跡**
```bash
# 行261-263: 変数がクォートされていない
[ -n "$IOS_FW" ] && [ -d "$IOS_FW" ] && ARGS="$ARGS -framework $IOS_FW"
[ -n "$IOS_SIM_FW" ] && [ -d "$IOS_SIM_FW" ] && ARGS="$ARGS -framework $IOS_SIM_FW"
[ -n "$MACOS_FW" ] && [ -d "$MACOS_FW" ] && ARGS="$ARGS -framework $MACOS_FW"

# 行268: ARGSがクォートされていない（意図的な可能性あり）
xcodebuild -create-xcframework $ARGS -output "$OUTPUT_DIR/${MODULE}.xcframework"
```

**推奨対策**
```bash
# ARGSを配列として構築
declare -a ARGS=()
[ -n "$IOS_FW" ] && [ -d "$IOS_FW" ] && ARGS+=(-framework "$IOS_FW")
[ -n "$IOS_SIM_FW" ] && [ -d "$IOS_SIM_FW" ] && ARGS+=(-framework "$IOS_SIM_FW")
[ -n "$MACOS_FW" ] && [ -d "$MACOS_FW" ] && ARGS+=(-framework "$MACOS_FW")

xcodebuild -create-xcframework "${ARGS[@]}" -output "$OUTPUT_DIR/${MODULE}.xcframework"
```

**参考**
- [ShellCheck SC2086](https://www.shellcheck.net/wiki/SC2086)

---

### [SEV-002] Medium: 引数バリデーションの欠如

**概要**
コマンドライン引数 `$1` が検証なしで使用されており、任意の値を `MODULES` 配列に設定できます。

**影響**
悪意のある入力（例：`../../etc/passwd`）がモジュール名として処理される可能性があります。ただし、実際には `xcodebuild -scheme` に渡されるため、Xcode側で無効なスキーム名としてエラーになります。直接的なコマンドインジェクションは発生しません。

**場所**
- ファイル: `/Users/hakaru/Desktop/Develop/MIDI2Kit/Scripts/build-xcframework.sh`
- 行: 21-25

**証跡**
```bash
# 入力バリデーションなし
if [ -n "$1" ]; then
    MODULES=("$1")
else
    MODULES=("${ALL_MODULES[@]}")
fi
```

**推奨対策**
```bash
# 許可リストによるバリデーション
if [ -n "$1" ]; then
    # Check if input is in allowed modules list
    VALID=false
    for allowed in "${ALL_MODULES[@]}"; do
        if [ "$1" = "$allowed" ]; then
            VALID=true
            break
        fi
    done
    if [ "$VALID" = false ]; then
        echo "Error: Invalid module '$1'. Allowed: ${ALL_MODULES[*]}"
        exit 1
    fi
    MODULES=("$1")
else
    MODULES=("${ALL_MODULES[@]}")
fi
```

**参考**
- [OWASP Command Injection](https://owasp.org/www-community/attacks/Command_Injection)

---

### [SEV-003] Low: rm -rf コマンドの使用

**概要**
`rm -rf` コマンドが固定パスに対して使用されています。変数展開の問題により、意図しないディレクトリが削除される可能性があります。

**影響**
現在の実装では `BUILD_DIR` は `/tmp/midi2kit-build` にハードコードされており、リスクは低いです。

**場所**
- ファイル: `/Users/hakaru/Desktop/Develop/MIDI2Kit/Scripts/build-xcframework.sh`
- 行: 32, 267

**証跡**
```bash
# 行32: 固定パス - 安全
rm -rf "$BUILD_DIR"

# 行267: 変数展開 - クォート済みで安全
rm -rf "$OUTPUT_DIR/${MODULE}.xcframework"
```

**推奨対策**
現在の実装は適切にクォートされており、修正不要です。ただし、防御的プログラミングとして以下を検討できます。

```bash
# 削除前にパス検証
if [[ "$BUILD_DIR" != /tmp/* ]]; then
    echo "Error: BUILD_DIR must be under /tmp"
    exit 1
fi
rm -rf "$BUILD_DIR"
```

---

### [SEV-004] Low: find コマンドの出力を変数に格納

**概要**
`find` コマンドの出力が `head -1` で1件に制限されていますが、パス内の改行やスペースを考慮していません。

**影響**
フレームワークパスに改行やスペースが含まれる場合（通常はない）、パス解析が誤動作する可能性があります。

**場所**
- ファイル: `/Users/hakaru/Desktop/Develop/MIDI2Kit/Scripts/build-xcframework.sh`
- 行: 87-95

**証跡**
```bash
local IOS_FW=$(find "$MODULE_BUILD/ios" -path "*Release-iphoneos*PackageFrameworks*" -name "${SCHEME}.framework" -type d 2>/dev/null | head -1)
```

**推奨対策**
```bash
# -print0 と read -d '' で安全に処理
local IOS_FW=""
while IFS= read -r -d '' fw; do
    IOS_FW="$fw"
    break
done < <(find "$MODULE_BUILD/ios" -path "*Release-iphoneos*PackageFrameworks*" -name "${SCHEME}.framework" -type d -print0 2>/dev/null)
```

---

### [SEV-005] Low: cd コマンドの失敗時処理

**概要**
`cd` コマンドが失敗した場合、後続のコマンドが意図しないディレクトリで実行される可能性があります。

**影響**
`$OUTPUT_DIR` が存在しない場合、`zip` コマンドがカレントディレクトリで実行されます。ただし、`set -e` によりスクリプトは終了します。

**場所**
- ファイル: `/Users/hakaru/Desktop/Develop/MIDI2Kit/Scripts/build-xcframework.sh`
- 行: 272-274

**証跡**
```bash
cd "$OUTPUT_DIR"
zip -r -q "${MODULE}.xcframework.zip" "${MODULE}.xcframework"
cd - > /dev/null
```

**推奨対策**
```bash
# サブシェルで実行
(cd "$OUTPUT_DIR" && zip -r -q "${MODULE}.xcframework.zip" "${MODULE}.xcframework")
```

---

### [SEV-006] Info: デバッグ情報の出力

**概要**
ビルドプロセスで詳細なパス情報が出力されています。ローカルビルドスクリプトでは問題ありませんが、CI/CD環境では機密パスが漏洩する可能性があります。

**影響**
開発環境のディレクトリ構造が外部に公開される可能性があります。

**場所**
- ファイル: `/Users/hakaru/Desktop/Develop/MIDI2Kit/Scripts/build-xcframework.sh`
- 行: 98-101, 255-256

**証跡**
```bash
echo "  🔍 Found frameworks:"
[ -n "$IOS_FW" ] && echo "    📱 iOS: $IOS_FW" || echo "    📱 iOS: NOT FOUND"
```

**推奨対策**
CI環境では `-q` フラグでquietモードを実装することを検討。

---

### [SEV-007] Info: Package.swift - 外部依存関係

**概要**
`swift-docc-plugin` への依存が存在します。サプライチェーンセキュリティの観点から記録します。

**影響**
Apple公式リポジトリからの依存であり、リスクは最小限です。

**場所**
- ファイル: `/Users/hakaru/Desktop/Develop/MIDI2Kit/Package.swift`
- 行: 72

**証跡**
```swift
dependencies: [
    .package(url: "https://github.com/apple/swift-docc-plugin", from: "1.0.0"),
],
```

**推奨対策**
依存関係の定期的な監査とバージョン固定を検討。

---

## 良好な実装

以下のセキュリティ上の良好な実装が確認されました。

1. **`set -e` の使用** (行14): エラー時に即座にスクリプトが終了
2. **ハードコードされたビルドディレクトリ** (行28): `/tmp/midi2kit-build` - ユーザー入力に依存しない
3. **変数の適切なクォーティング** (大部分): `"$BUILD_DIR"`, `"$OUTPUT_DIR"` 等
4. **`local` 変数の使用** (行37等): 変数スコープの適切な管理
5. **エラー出力の抑制** (行87等): `2>/dev/null` で存在しないパスのエラーを適切に処理
6. **ファイル存在チェック** (行109等): 操作前に `-d` でディレクトリ存在を確認

---

## 推奨事項（優先順位順）

1. **短期（次回リリース）**:
   - [SEV-002] 引数バリデーションの追加（許可リスト方式）

2. **中期**:
   - [SEV-001] ARGS変数を配列として構築
   - [SEV-005] cdをサブシェルに変更

3. **低優先度**:
   - [SEV-003], [SEV-004] 現在の実装で実質的なリスクなし
   - [SEV-006], [SEV-007] 情報提供のみ

---

## 監査対象外・制限事項

- xcodebuildコマンド自体のセキュリティ（Apple提供ツール）
- Swift Package Manager の内部処理
- macOS/iOSのサンドボックス機構
- ネットワーク通信（このスクリプトは外部通信を行わない）

---

## 結論

本監査対象のコードは、ローカルビルドスクリプトとして適切なセキュリティレベルを維持しています。主な懸念事項は引数バリデーションの欠如ですが、xcodebuildへの間接的な渡しとなるため、実質的なコマンドインジェクションリスクは低いと評価されます。

Product rename（MIDI2ClientDynamic -> MIDI2KitDynamic）およびmacOS Versions/A/ 対応の変更は、セキュリティ上の問題を導入していません。
