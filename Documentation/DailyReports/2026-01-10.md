# 日報 2026-01-10

## 時間管理

**作業時間:** 15:22 - 00:30 + 09:30-10:30 + 10:45-11:00 + 11:30-17:00 (約16時間)

| 時間 | 作業内容 | 所要時間 |
|------|---------|---------|
| 15:22-16:30 | MIDI2Kit設計・Package.swift作成 | 1h 10m |
| 16:30-18:00 | MIDI2Core実装 (MUID, DeviceIdentity, Mcoded7, Constants) | 1h 30m |
| 18:00-19:00 | MIDI2CI実装 (DiscoveredDevice, MessageBuilder, Parser) | 1h |
| 19:00-20:30 | MIDI2PE実装 (PEResource, PETypes, ChunkAssembler, RequestIDManager) | 1h 30m |
| 20:30-21:30 | MIDI2Transport実装 (MIDITransport, CoreMIDI, Mock, SysExAssembler) | 1h |
| 21:30-22:30 | テスト作成・デバッグ (48テスト) | 1h |
| 22:30-23:30 | PETransactionManager追加・リーク防止対策 | 1h |
| 23:30-00:00 | CoreMIDITransport改善 (二重接続防止、パフォーマンス) | 30m |
| 00:00-00:30 | ドキュメント作成 (README, Architecture, API, BestPractices) | 30m |
| 09:30-10:30 | CIManager統合、エラー検出改善、ログユーティリティ | 1h |
| 10:45-11:10 | バグ修正5件 + タイムアウトテスト修正 | 25m |
| 11:30-13:00 | PETransactionManager責務分離リファクタリング | 1h 30m |
| 13:00-14:30 | PESubscriptionManager実装 (自動再接続) | 1h 30m |
| 14:30-15:30 | CIManager Destination mapping修正・Events追加 | 1h |
| 15:30-16:30 | Per-device inflight limiting実装 | 1h |
| 16:30-17:00 | テスト修正・ドキュメント更新 | 30m |

**合計:** 約16時間

---

## セッション6 作業内容 (16:30-17:00)

### テスト修正: Per-device inflight limiting対応

**問題:**
`stopReceiving releases all Request IDs` と `stopReceiving handles many concurrent requests safely` テストが失敗。新しいper-device inflight limiting機能（デフォルト: maxInflightPerDevice=2）により、5リクエスト中2つのみがactiveになり、残り3つはwaiterキューで待機。

**修正:**
```swift
// Before: 全リクエストがPendingになることを期待
#expect(diagBefore.contains("Pending requests: 5"))

// After: inflight + waiting = total を検証
#expect(diagBefore.contains("inflight=2, waiting=3"))
```

---

## セッション5 作業内容 (15:30-16:30)

### Per-device Inflight Limiting 実装

**問題:**
一部のMIDIデバイス（例: KORG Module Pro）は同時に多数のPEリクエストを処理できない。多数の同時リクエストを送ると、レスポンスが欠落したり、チャンクが壊れたりする。

**解決:**
`PETransactionManager` に per-device inflight limiting を実装：

```swift
public actor PETransactionManager {
    /// Maximum concurrent in-flight requests per device
    public nonisolated let maxInflightPerDevice: Int  // デフォルト: 2
    
    /// Per-device inflight state
    private var deviceInflightState: [MUID: DeviceInflightState] = [:]
    
    private struct DeviceInflightState {
        var currentInflight: Int = 0
        var waiters: [CheckedContinuation<Void, Never>] = []
    }
    
    public func begin(...) async -> UInt8? {
        // Wait for device slot if at capacity
        await waitForDeviceSlot(destinationMUID)
        // ... acquire RequestID
    }
    
    public func cancel(requestID: UInt8) {
        // ... release RequestID
        releaseDeviceSlot(transaction.destinationMUID)  // Resume next waiter
    }
}
```

**動作:**
- デバイスあたり最大2リクエスト（デフォルト）まで同時実行
- 超過リクエストはFIFOキューで待機
- 先行リクエスト完了時に自動で次のwaiterを起動
- 異なるデバイスへの同時リクエストは影響なし

---

## セッション4 作業内容 (11:30-15:30)

### 1. PETransactionManager責務分離リファクタリング

**問題:**
`PETransactionManager` と `PEManager` の両方がタイムアウト管理とCompletion通知を担当していた。この「責務の重複」が将来のバグの温床になりうる。

**リファクタリング後の責務分担:**

| 責務 | PETransactionManager | PEManager |
|------|:-------------------:|:---------:|
| Request ID 管理 | ✅ | ❌ |
| チャンク組み立て | ✅ | ❌ |
| トランザクション追跡 | ✅ | ❌ |
| **Continuation管理** | ❌ | ✅ |
| **タイムアウト管理** | ❌ | ✅ |
| **レスポンス配信** | ❌ | ✅ |

**PETransactionManagerから削除:**
- `complete(requestID:header:body:)`
- `completeWithError(requestID:status:message:)`
- `checkTimeouts()` / `startMonitoring()` / `stopMonitoring()`
- `waitForCompletion(requestID:)`
- `PEMonitoringConfiguration` / `PEMonitorHandle`
- `completionHandlers` dictionary

### 2. PESubscriptionManager実装

**新機能:** デバイス再接続時の自動再購読サポート

```swift
public actor PESubscriptionManager {
    public var resubscribeDelay: Duration = .milliseconds(500)
    public var maxRetryAttempts: Int = 3
    
    // Subscribe with device identity for matching after MUID change
    @discardableResult
    public func subscribe(
        to resource: String,
        on muid: MUID,
        identity: DeviceIdentity?
    ) async throws -> UUID
    
    // Unified event stream (survives reconnections)
    public var events: AsyncStream<PESubscriptionEvent>
}

public enum PESubscriptionEvent: Sendable {
    case subscribed(intentID: UUID, subscribeId: String)
    case suspended(intentID: UUID, reason: String)
    case restored(intentID: UUID, newSubscribeId: String)
    case failed(intentID: UUID, reason: String)
    case notification(PENotification)
}
```

**特徴:**
- InvalidateMUID処理対応
- デバイスIdentityによるマッチング（MUID変更後も追跡可能）
- 設定可能なリトライ回数・遅延
- 統一イベントストリーム

### 3. CIManager Destination Mapping修正

**問題:**
`findDestination(for:)` が `MIDIDestinationID(sourceID.value)` としていたが、CoreMIDIではsourceとdestinationのendpoint refは通常異なる。

**修正:** Entity経由のマッピングに変更
```swift
public func destination(for muid: MUID) async -> MIDIDestinationID? {
    guard let entry = devices[muid],
          let sourceID = entry.sourceID else { return nil }
    
    // Find entity containing this source
    var entity: MIDIEntityRef = 0
    guard MIDIEndpointGetEntity(sourceID.value, &entity) == noErr else { return nil }
    
    // Get destination from same entity
    if MIDIEntityGetNumberOfDestinations(entity) > 0 {
        return MIDIDestinationID(MIDIEntityGetDestination(entity, 0))
    }
    return nil
}
```

### 4. MUID Validation修正

**問題:** テストコードで `MUID(rawValue: 0x12345678)!` を使用 → 28ビット制限（0x0FFFFFFF）を超過 → クラッシュ

**修正:**
```swift
// Before (クラッシュ)
let muid = MUID(rawValue: 0x12345678)!  // > 0x0FFFFFFF

// After (OK)
let muid = MUID(rawValue: 0x01234567)!  // <= 0x0FFFFFFF
```

---

## セッション3 作業内容 (10:45-11:10)

### 修正1: PEManager.stopReceiving() RequestIDリーク & 安全でない辞書操作

**問題:**
1. `stopReceiving()` が `transactionManager.cancelAll()` を呼ばず、Request IDがリーク
2. `pendingContinuations` をイテレート中に変更 → クラッシュの危険

**修正:**
```swift
// Before (リーク & 安全でない)
for (requestID, continuation) in pendingContinuations {
    pendingContinuations.removeValue(forKey: requestID)  // ❌
    continuation.resume(throwing: PEError.cancelled)
}

// After (安全)
for continuation in pendingContinuations.values {
    continuation.resume(throwing: PEError.cancelled)
}
pendingContinuations.removeAll()
await transactionManager.cancelAll()  // ✅ RequestID解放
```

### 修正2: CoreMIDITransport パケット順序問題

**問題:** 各パケットで別々の Task を生成 → 順序保証なし → SysEx が壊れる

**修正:**
```swift
// Before (順序保証なし)
for packet in packets {
    Task { await processReceivedData(data) }  // ❌ 順序不定
}

// After (順序保証あり)
var allPacketData: [[UInt8]] = []
for packet in packets { allPacketData.append(data) }
Task {
    for data in allPacketData { await processReceivedData(data) }  // ✅
}
```

### 修正3: PETransactionManager.waitForCompletion() continuation上書き

**問題:** 同じ requestID に複数回 `waitForCompletion()` を呼ぶと、前の continuation が上書き

**修正:**
```swift
if completionHandlers[requestID] != nil {
    return .cancelled  // 即座に返す
}
```

### 修正4: PEManager タイムアウト二重管理の解消

`PEManager.timeoutTasks` に一本化。

### 修正5: CoreMIDITransport sourceID が常に nil

**修正:** `MIDIPortConnectSource` の `connRefCon` パラメータを使用
```swift
let connRefCon = UnsafeMutableRawPointer(bitPattern: UInt(sourceRef))
MIDIPortConnectSource(inputPort, sourceRef, connRefCon)
```

### 修正6: タイムアウトテスト再有効化

`GET times out when no reply` テストを `.disabled` から有効化。全てパス。

---

## セッション2 作業内容 (09:30-10:30)

### 1. CIManager統合
MIDI2CIとMIDI2Kit両方に存在していた`CIManager`を単一実装に統合。

### 2. CoreMIDI送信エラー検出改善
- `packetListAddFailed(dataSize:bufferSize:)` 追加
- `packetListEmpty` 追加

### 3. unknownRequestID と timeout の分離
```swift
public enum PEChunkResult {
    case incomplete(received: Int, total: Int)
    case complete(header: Data, body: Data)
    case timeout(requestID: UInt8, received: Int, total: Int, partial: Data?)
    case unknownRequestID(requestID: UInt8)  // NEW
}
```

### 4. MIDI2LogUtils 追加
安全な構造化ログユーティリティ。

---

## セッション1 作業内容 (15:22-00:30)

MIDI2Kit ライブラリの新規作成。詳細は省略。

---

## 成果物

| 項目 | 状態 |
|------|------|
| MIDI2Kit Package | ✅ 作成完了 |
| MIDI2Core | ✅ 実装完了 |
| MIDI2CI | ✅ 実装完了 (CIManager統合済み、Events追加) |
| MIDI2PE | ✅ 実装完了（TransactionManager責務分離済み、SubscriptionManager追加） |
| MIDI2Transport | ✅ 実装完了（差分接続対応、sourceID対応、Entity mapping） |
| ユニットテスト | ✅ 142+テスト (全パス) |
| ドキュメント | ✅ README + Architecture + API + BestPractices + CHANGELOG |

## 本日のコミット履歴

1. `Initial commit: MIDI2Kit v0.1.0`
2. `Add comprehensive documentation`
3. `Unify CIManager implementations and fix PEManager timeout`
4. `Improve error detection and add auto-start monitoring`
5. `Add unknownRequestID result type and improve logging utilities`
6. `Fix: PEManager.stopReceiving() RequestID leak and unsafe mutation`
7. `Fix: CoreMIDITransport packet ordering for SysEx assembly`
8. `Fix: Prevent waitForCompletion continuation overwrite`
9. `Refactor: Unify timeout management in PEManager`
10. `Fix: Populate MIDIReceivedData.sourceID in CoreMIDITransport`
11. `Fix: Re-enable GET timeout test`
12. `Consolidate timeout/continuation management in PEManager`
13. `Add PESubscriptionManager with auto-reconnection support`
14. `Fix: CIManager source-to-destination mapping via Entity`
15. `Add per-device inflight limiting to PETransactionManager`
16. `Fix: MUID validation in PEDeviceHandleTests`
17. `Fix: Update stopReceiving tests for inflight limiting`

## 課題管理

### 解決済み ✅
- [x] RequestIDリーク問題
- [x] SysExパケット順序問題
- [x] Continuation上書き問題
- [x] タイムアウト二重管理
- [x] sourceID未設定問題
- [x] タイムアウトテストのSegfault
- [x] Source-to-Destinationマッピング問題
- [x] MUID 28ビット制限違反
- [x] デバイス過負荷問題（Per-device inflight limiting）

### 次のステップ 📋
- [ ] SimpleMIDIControllerへのMIDI2Kit統合
- [ ] Subscription Notify実テスト（実機確認）
- [ ] PE Set APIの完全実装

## KPT

### Keep (継続)
- モジュール分割によるテスタビリティ向上
- 問題発見→対策のドキュメント化
- エラー種別の明確な分離（timeout vs unknownRequestID）
- 包括的なテストカバレッジ
- 責務分離の徹底

### Problem (問題)
- SimpleMIDIControllerとの重複コード（将来的に統合必要）
- 一部のデバイス固有の問題（KORG Module Proのチャンク欠落など）

### Try (試行)
- SimpleMIDIControllerへのMIDI2Kit統合
- 実デバイスでのSubscription/Notifyテスト
- パフォーマンスベンチマーク

## 関連リソース

- MIDI2Kit: `~/Desktop/MIDI2Kit/`
- SimpleMIDIController: `~/Desktop/SimpleMidiController/` (release/1.1.0ブランチ)
- ドキュメント: `~/Desktop/MIDI2Kit/Documentation/`
